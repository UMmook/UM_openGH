let a = 1;
let b = 2 + a;
console.log(b);

let c;  // 선언
c = 1;  // 대입
console.log(c);

let C = 10;
let d = c + 1;
console.log(d);

// error case
// let let = 1;

// sum = 1 + 2 + 3;
// sum([1, 2, 3]);


totalArea = 10;  // (o)
TotalArea = 10;  // (x : class name)
Total_Area = 10; // (x)
total_area = 10; // (o)